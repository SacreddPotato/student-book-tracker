# Development defaults to http://127.0.0.1:8787 when this value is omitted.
# Production builds must set the deployed production sync API URL before packaging.
VITE_SYNC_API_BASE_URL=
# Optional shared transport guard for the sync API. This is not a user identity or Neon credential.
VITE_SYNC_API_SHARED_SECRET=
